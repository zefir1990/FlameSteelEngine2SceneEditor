// swift-tools-version: 5.9
import PackageDescription

let package = Package(
    name: "EBox",
    products: [
        .library(name: "EBox", targets: ["EBox"])
    ],
    targets: [
        .target(name: "EBox")
    ]
)
